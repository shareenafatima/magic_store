<section class="section has-text-white-ter bg-dark">
<div calss="columns">
<div class="column is-8 ">
<h1><%= @instrument.title %></h1>
</div>
</div>
</section>

<ul class="pv1">
<% if @instrument.brand? %>
<li class="inline-block pr3"><% @instrument.brand %></li>
<% end %>

<li class="inline-block pr3"><% @instrument.model %></li>

<% if @instrument.condition? %>
<li class="inline-block pr3"><% @instrument.condition %></li>
<% end %>
</ul>

<div class="feature-image">
<%= image_tag(@instrument.image_url(:default)) %>
</div>

<div class="content pa4 mt3 bg-white border-radius-3">
<%= @instrument.description %>
</div>

<h3 class="subtitle is-4 pt5">Product Specs</h3>

<table class="table is-narrow">
<% if @instrument.condition %>
<tr>
<% end %>

<% if @instrument.finish %>
<tr>
<td class=" has-text-weight-bold">Finish:</td>
<td><%= @instrument.finish %>
</tr>
<% end %>

<% if @instrument.brand %>
<tr>
<td class=" has-text-weight-bold">Brand:</td>
<td><%= @instrument.brand %>
</tr>
<% end %>

<tr>
<td class=" has-text-weight-bold">Model:</td>
<td><%= @instrument.model %>
</tr>
</table>

<div class="column is-3 is-offset-1">
<div class="bg-white pa4 border-radius">
<h4 class="title is-5" ><%= number_to_currency(@instrument.price) %></h4>
<%= button_to 'Add to cart', '#', class:'button is-warning add-to-cart' %>
</div>
</div>

<% if instrument_author(@instrument) %>
<%= link_to 'Edit', edit_instrument_path(@instrument), class:'button' %>
<% end %>


<%
=begin%>
 <%= render @instrument %>

<div>
  <%= link_to "Edit this instrument", edit_instrument_path(@instrument) %> |
  <%= link_to "Back to instruments", instruments_path %>

  <%= button_to "Destroy this instrument", @instrument, method: :delete %>
</div> 
<%
=end%>
